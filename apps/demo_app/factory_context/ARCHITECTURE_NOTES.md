# Architecture Notes

The demo application intentionally has no architecture yet. Its initial purpose
is to validate the construction-site boundary:

- application code belongs in `app/`
- application documentation belongs in `docs/`
- tests belong in `tests/`
- factory-specific state remains in the `factory_*` directories
