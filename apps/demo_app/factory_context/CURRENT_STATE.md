# Current State

The demo application is an inert Phase 1 workbench. It contains documentation,
context, task records, report storage, prompt storage, and helper-script
storage. It does not contain implementation code.
