__pycache__/
*.py[cod]

# Runtime control flags and the mission lock are transient, not committed.
state/*.flag
state/mission.lock

# Seed-grown project state is runtime output; the engine recreates it.
factory_state/projects/
